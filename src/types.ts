export type User = {
        username?: string;
        email: string;
        password: string;
};
export type UserT = {
        username: string;
        email: string;
        password: string;
};

export type UserToken = {
        userId: string;
        username: string;
};
